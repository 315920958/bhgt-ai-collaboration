# 项目结构与职责说明

> 本文档说明本产品涉及的全部仓库及其职责。写法与同目录其他文档保持一致，供开发、运营与 AI 协作参考。

## 一、总览

本产品共涉及 **4 个仓库**，其中 **3 个为代码工程**（实际编写、运行业务代码），另外 **1 个为 AI 协作文档仓库**（不放代码）。

### 1. 三个代码工程（统称「BHGT 三端」）

> **统称说明**：`bhgt-h5-client`、`bhgt-admin`、`bhgt-server` 这三个代码工程，合称为 **「BHGT 三端」**（用户端 + 后台管理端 + 服务端）。后续协作中，凡提到「三端」「BHGT 三端」「代码工程」，即指这三个仓库，不含 AI 协作文档仓库。

| 仓库 | 定位 | 是否代码工程 | 远程仓库 | 分支 |
| --- | --- | --- | --- | --- |
| **bhgt-h5-client** | 用户端（玩家端） | ✅ 是 | `git@github.com:315920958/bhgt-h5-client.git` | `develop` |
| **bhgt-admin** | 后台管理端（运营 / 管理员） | ✅ 是 | `git@github.com:315920958/bhgt-admin.git` | `develop` |
| **bhgt-server** | 服务端（后端 API） | ✅ 是 | `git@github.com:315920958/bhgt-server.git` | `develop` |

### 2. 一个 AI 协作文档仓库（非代码工程）

| 仓库 | 定位 | 是否代码工程 | 远程仓库 | 分支 |
| --- | --- | --- | --- | --- |
| **bhgt-ai-collaboration** | AI 协作与文档仓库（本文档所在仓库） | ❌ 否 | `git@github.com:315920958/bhgt-ai-collaboration.git` | `main` |

**架构关系：**

- `bhgt-h5-client`（玩家玩）与 `bhgt-admin`（运营配）都通过 HTTP 调用 `bhgt-server`；
- `bhgt-server` 统一接 MongoDB 存取数据；
- `bhgt-ai-collaboration` 不放代码，只放需求文档、原型图、AI 协作指令与自定义技能。

---

## 二、bhgt-h5-client（用户端 / 玩家端）

| 项 | 说明 |
| --- | --- |
| 角色 | 面向终端玩家的 H5 网页应用（手机浏览器内运行的修仙文字 / 剧情养成游戏） |
| 现状 | 已初始化：React 18 + Vite + TypeScript，开发端口 **4002** |
| 已配置 | `/api` 代理到 `http://localhost:4001`；axios 实例 `baseURL` 默认 `/api` |
| 技术栈 | React 18 + Vite + TypeScript + axios |
| 作用 | 玩家在此进行游戏（看剧情、战斗、选天赋、商店、重生等）；通过接口调用 `bhgt-server` 获取数据、保存存档 |
| 远程仓库 | `git@github.com:315920958/bhgt-h5-client.git` |
| 当前分支 | `develop`（另含 main / release / production 分支） |

---

## 三、bhgt-admin（后台管理端）

| 项 | 说明 |
| --- | --- |
| 角色 | 运营 / 管理员使用的后台管理系统前端 |
| 现状 | 已初始化：Vue 3 + Vite + TypeScript，开发端口 **4000** |
| 已配置 | `/api` 代理到 `http://localhost:4001`；axios 实例 `baseURL` 默认 `/api` |
| 技术栈 | Vue 3 + Vite + TypeScript + axios |
| 作用 | 在界面上配置所有后台模块——属性、天赋、境界、节点、按钮、物品、商品、CG、广告等（配置项详见同目录《Demo后台管理模块与配置项.md》）；配置经 `bhgt-server` 落库（MongoDB） |
| 远程仓库 | `git@github.com:315920958/bhgt-admin.git` |
| 当前分支 | `develop`（另含 main / release / production 分支） |

---

## 四、bhgt-server（服务端 / API）

| 项 | 说明 |
| --- | --- |
| 角色 | 后端服务，统一提供数据接口与业务逻辑 |
| 现状 | 已初始化：NestJS 11，端口 **4001** |
| 已配置 | `setGlobalPrefix('api')`、`enableCors()`；`GET /api` 返回 `Hello World!` |
| 数据库 | 计划接入 MongoDB（Ubuntu/Debian 用 apt 安装，macOS 用 brew，或统一用 Docker） |
| 技术栈 | NestJS 11 + TypeScript，计划用 Mongoose 接 MongoDB |
| 作用 | 给 `bhgt-admin` 与 `bhgt-h5-client` 同时提供 API；负责配置读写、玩家存档、游戏逻辑校验 |
| 远程仓库 | `git@github.com:315920958/bhgt-server.git` |
| 当前分支 | `develop`（另含 main / release / production 分支） |

---

## 五、bhgt-ai-collaboration（AI 协作与文档仓库）

| 项 | 说明 |
| --- | --- |
| 角色 | 本文档所在仓库，**不放业务代码** |
| 作用 | 集中存放 AI 协作指令、自定义技能、项目文档（需求 / 设定 / 原型图等） |
| 远程仓库 | `git@github.com:315920958/bhgt-ai-collaboration.git` |
| 当前分支 | `main` |
| 已有文档 | `项目文档/Demo后台管理模块与配置项.md`、`项目文档/完整剧情设定及系统设定v0.0.2.md`、本文档 |
| 目录结构 | `项目文档/`（需求 / 设定 / 文档）、`界面/`（UI 相关视觉素材，见下） |

### 界面相关素材目录

本仓库除 `项目文档/` 外，另设 `界面/` 目录，集中存放游戏 UI 相关的视觉素材（**不放代码**，仅放图片 / 设计资源）：

| 目录 | 用途 |
| --- | --- |
| `界面/原型图` | **UI 原型 / 线框图**。设计阶段产出的界面草图、交互原型（一般是 Figma / 墨刀等工具导出的原型截图），用于评审玩法流程与页面布局 |
| `界面/导出图` | **设计终稿导出图**。从设计工具导出的成品素材——最终界面图、切图、图标等，供开发对照或运营展示 |

> 说明：`界面/` 下两个子目录当前以 `.gitkeep` 占位保留结构，待放入真实图片素材后再提交。

---

## 六、端口与通讯约定

| 服务 | 端口 | 说明 |
| --- | --- | --- |
| bhgt-admin | 4000 | 前端开发服务器；`/api` 经 Vite 代理转发到 4001 |
| bhgt-h5-client | 4002 | 用户端开发服务器；`/api` 经 Vite 代理转发到 4001 |
| bhgt-server | 4001 | 后端 API；对外统一前缀 `/api`，已开启 CORS |

> 前后端通讯方式：开发环境由 Vite 代理转发；生产环境由 `bhgt-admin` / `bhgt-h5-client` 直接请求 `bhgt-server` 地址（后端已开 CORS）。

---

## 七、分支策略约定

**适用范围**：仅限三个代码工程 `bhgt-h5-client`、`bhgt-admin`、`bhgt-server`。文档仓库 `bhgt-ai-collaboration` 不参与此分支模型，仅用 `main`。

三个代码工程统一采用 **四分支模型**，均从初始主干 `main` 切出：

| 分支 | 角色 | 说明 |
| --- | --- | --- |
| `main` | 初始主干 | 首次提交基线，所有分支的源头 |
| `develop` | 开发分支 | 日常开发基线；当前**本地默认所在分支**，已推送到服务端 |
| `release` | 预发布分支 | 发版前的集成 / 测试分支 |
| `production` | 生产分支 | 线上稳定版本分支 |

> 初始化方式：三个分支均从 `main` 当前代码切出（初始内容与 `main` 一致），并均已推送到 GitHub。本地已切换至 `develop` 分支进行后续开发。
